{
  "name": "cookie-crumbs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && node --check receipt.mjs",
    "test": "node --test tests/*.test.mjs",
    "ci": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
