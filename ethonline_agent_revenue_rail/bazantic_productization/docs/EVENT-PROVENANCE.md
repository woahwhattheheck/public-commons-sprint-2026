# Event provenance

Agent Revenue Rail is a Start-Fresh ETHOnline 2026 build. Lane C was developed during the event as the Bazantic/productization layer. It is intentionally isolated from pre-event application source and consumes Lane A / Lane B through explicit evidence contracts rather than copying unrelated project code.

For final publication, preserve:

- first commit timestamp and complete git history of each lane;
- exact imported Lane A / Lane B commit identities;
- exact sponsor/API documentation versions used for implementation decisions;
- deployment and live-test timestamps;
- exact transaction, Graph-query, Recipe/Gateway, A/B and video evidence digests;
- any rejoin/integration commits as distinct commits rather than squashing away provenance when event rules or judgeability benefit from visible history.

This file is provenance guidance, not independent proof of Start-Fresh eligibility. Public repository history and event records remain controlling evidence.
